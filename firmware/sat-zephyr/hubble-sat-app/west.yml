# Copyright (c) 2026 HubbleNetwork
# SPDX-License-Identifier: Apache-2.0

manifest:

  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos
    - name: hubble
      url-base: https://github.com/HubbleNetwork

  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      # Same pin as the BLE workspace (firmware/zephyr). The satellite SoC
      # driver in the SDK selects its nrfx_timer reference on ZEPHYR_VERSION,
      # and v4.4.0 is the branch it was validated against.
      revision: v4.4.0
      import: true
      west-commands: scripts/west-commands.yml
    - name: hubblenetwork-sdk
      repo-path: sdk
      # v3.0.0 is the first tag containing 370f513 "sat: packet: Fix symbol
      # clobbered by ECC lookup", without which every transmitted packet has
      # its last 6 data symbols overwritten by Reed-Solomon parity and cannot
      # be decoded or decrypted by the ground station. Do not move this pin
      # below v3.0.0.
      revision: v3.0.0
      path: modules/lib/hubblenetwork-sdk
      remote: hubble
